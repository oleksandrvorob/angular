import { ClickCatcherDirective } from './click-catcher.directive';

describe('ClickCatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickCatcherDirective();
    expect(directive).toBeTruthy();
  });
});
